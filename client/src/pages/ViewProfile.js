import React from 'react'

function ViewProfile({ user }) {
    const { first_name, last_name, username, profile_img, experience, email, plan_id} = user 

  return (

    <>

    </>
  )
}

export default ViewProfile